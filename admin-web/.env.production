# Public API base for production Admin Web builds (safe to commit).
# Override in Vercel Project Settings if the Railway hostname changes.
NEXT_PUBLIC_API_BASE_URL=https://yelo-laundry-apps-production-2e03.up.railway.app/api/v1
